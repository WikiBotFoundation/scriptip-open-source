#include <view>
#include <data>
#include <programminglanguage>
   struct(ram == script)
      data (ram == view
              lexer.init(py)
         view.comfy == view.py(ram)
#include <script>
          iostream-complex( == py
     init(ip.host.data == ( == py
         confirm.view($, script_token % print % data
    init(ram.host(view.((viewer.py
                  coding((view.py % token
       print $.local(ascript( == INITPY
