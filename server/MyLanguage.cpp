#include <lang>
#include <system>
#include <java>
  system.default( == key.lang)
     java.register( == lang(;key)
  system.files( == default.register )
      
  register.lang( ==> typename.keyword)
        files.default( ; manifest.lang)
    system.double(; files.lang)
          language.default
       print(keyword.system( ==> this.keyword)
      this.keyword( ==> files(=={java})
           double._Imaginary(; mySystem)
       code.(; type::: files.system)
           isLang.delete(; commands)
     create(; command.file)
       print(; keyword.manifest
   system(::: java
          default.typeid ::: manifest.system
       double.command(; function : )
             if(TokenCreated.:function)
          token.file(; MySystem.manifest)
      !(a-19), system.files !a(,i, 5,)
           