if(null.charm(py.atom
    enteredTextCreate.atom.(val)
          if(token.create(val)
   commandColor.token(setColor="enterColor"
        token.val(is created text.(enter symbol""
        EnterCreateText.api(color
